name: solar system overview
on:
    workflow_dispatch: 
    push: 
        branches: 
            - main 
            - 'feature/*'   
  

#env:
 # MONGO_URI: 'mongodb+srv://${{ vars.MONGO_USERNAME }}:${{ secrets.MONGO_PASSWORD }}@cluster0.kirie.mongodb.net/?retryWrites=true&w=majority'
 
  #MONGO_USERNAME: ${{ vars.MONGO_USERNAME }}
  #MONGO_PASSWORD: ${{ secrets.MONGO_PASSWORD}}
              

jobs:
    unit_test:  
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
            
            - name: setup nodeJS version:10
              uses: actions/setup-node@v4
              with:
                    node-version: 18
           
            - name: cache npm dependencies         
              uses: actions/cache@v4
              with:
                path: node_modules
                key: ${{ runner.os }}-node_modules-${{ hashFiles('package-lock.json') }}
        
            - name: install dependencies 
              run : npm install 
           
           # - name: unit test 
            #  id: nodejs-unit-test-step
             # run: npm test
                        
            #- name: archive test result 
               # if:  always()
              #uses: actions/upload-artifact@v4
              #with:
               #     name: test-result
                #    path: test-results.xml   
                
    coverage_code:
      runs-on: ubuntu-latest
      steps:
        - name: checkout repo
          uses: actions/checkout@v4
        
        - name: setup nodeJS version:10
          uses: actions/setup-node@v4
          with:
                node-version: 18  
             
        - name: cache npm dependencies         
          uses: actions/cache@v4
          with:
            path: node_modules
            key: ${{ runner.os }}-node_modules-${{ hashFiles('package-lock-.json') }} 
                
        - name: install dependencies 
          run : npm install
          
        - name: check code coverage
          continue-on-error: true
          run: npm run coverage

        - name: upload node-module
          uses: actions/upload-artifact@v4
          with:
                name: code-coverage-Result
                path: coverage
                retention-days: 5

